<div class="container">
  <h1>Welcome to URL Shortener!</h1>

  <%= form_tag(links_path, :method => 'post') do %>
    <%= text_field_tag(:url)  %>
    <%= submit_tag('Shorten') %>
  <% end %>

</div>
